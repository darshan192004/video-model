# Final review — 2026-09-25

Reviewed state: `e0a8976`..HEAD (Phases 0–6 authored; GPU/k3s live runs DEFERRED).

## Scope & method

Review by the code review checklist: correctness, readability, architecture,
security, performance across the delivered system (compose + k8s + tests +
docs). Verification battery below ran on this machine.

## Verification evidence

| Check | Result |
| --- | --- |
| Secrets sweep (real values in tracked files) | CLEAN — only `.env.example`/`*.example` placeholders + code *names* |
| `git check-ignore` on `.env`, `deploy/certs/`, `overlays/*/secrets.env` | all ignored |
| `docker compose config --quiet` on all 5 live combos | OK (no warnings) |
| `kubectl kustomize` render, base + k3s + production overlays | OK (18 / 23 / 21 objects) |
| `nginx -t` (digest-pinned image, real conf + certs) | syntax OK + test successful |
| `controlplane_unit.sh` | PASS |
| `workflow_schema.sh` (structure) | PASS (class check skips w/o backend) |
| `gpu-smoke.sh --validate-shape` (incl. negatives) | PASS |
| `verify-manifest-lockstep.sh` | PASS |
| `bash -n` on all `system/scripts/*.sh` + suites | PASS |
| shellcheck | NOT INSTALLED on this box — run it on the GPU/k3s box before the final gate; `bash -n` is green meanwhile |
| README / runbook command-path existence check | PASS (only globs + the gate-generated report remain intentionally absent) |
| Live suites (proxy_tls, e2e_spa cold, smoke, k8s-smoke, k3s-regression) | DEFERRED — no running k3s/compose stack on this machine; run via `bash system/scripts/gate.sh` (gate fails loudly if a VERIFIED row is red) |

## Findings

### Fixed during this review (new commits, not amends)
- nginx conf: added `limit_req` zone + `client_max_body_size`; verified `nginx -t`.
- k8s base: `allowPrivilegeEscalation:false` + dropped ALL caps on the four
  deployments (compose already had `cap_drop`/`no-new-privileges`).
- Digest pins: `nvidia/cuda:12.4.1-runtime-ubuntu22.04`, `node:20-alpine`,
  `python:3.12-slim`, `nginx:1.27-alpine`, `postgres:16-alpine` (digests
  captured from the live registries; nvcr.io unroutable from here, so the CUDA
  digest is the docker.io-resolved one matching the unpinned default).
- Kustomize modernity: `patches:` list replaces deprecation-path
  `patchesStrategicMerge`; `labels:` replaces `commonLabels`.

### Accepted with reason (documented posture, not bugs)
- **gate.sh preflight requires the k3s overlay** → correct by design ("fails
  loudly if any non-deferred check is red"); cold-HOST gate runs are not a use case.
- **`COMFY_FETCH` uses a `kubectl run` curl pod** (comfyui is ClusterIP-only) —
  the plan draft's `COMFY_FETCH="$BASE"` was wrong; the regression wires the
  correct in-cluster fetch.
- **Qwen-Image-2.1 = Qwen Research License, commercial NO-GO** — recorded in
  `docs/LICENSES.md` with mitigation (internal-only now; commercial requires a
  Qwen license or an Apache T2I swap). This is a product gate, not a code defect.
- **Base image digest pin for the CUDA base** references the docker.io-resolved
  digest; the same tag previously resolved there implicitly — no behavior change.
- **No-verification items re-run offline is impossible** (unit/workflow/validate
  re-ran; live suites require a stack).

## Disposition

**SHIP-WITH-CAVEATS**

- Ships now as a **private/internal** media system (compose CPU + k3s dev
  overlay; `gate.sh` is the re-certification entry point).
- Caveats (all documented, none silently green):
  1. **Commercial launch blocked** by the Qwen Research License until a separate
     license or model swap (docs/LICENSES.md). Wan 2.2 is Apache-2.0.
  2. Every GPU/real-weights/k3s-live check is DEFERRED to the 3090 box; run
     `bash system/scripts/gate.sh` there before treating acceptance as complete.
  3. `shellcheck` not run here (absent); run on the box.
- Spec deviations are conscious and recorded in the per-phase ledgers
  (`.superpowers/sdd/`); the k3s/production overlays render + structurally
  verify but were never applied to a live cluster by the dev host.