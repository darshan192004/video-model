apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: comfyui-output
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 20Gi
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: comfy-input
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 10Gi
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gallery-storage
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 100Gi
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: uploads-storage
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 20Gi
---
# Placeholder the base comfyui pod mounts as its `models` volume; overlays
# replace the whole volume (dev hostPath, production CSI/PV) — kept for shape.
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: models-scratch
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 1Gi