apiVersion: apps/v1
kind: Deployment
metadata:
  name: comfyui
spec:
  template:
    spec:
      containers:
        - name: comfyui
          env:
            - name: COMFY_MODE
              value: "cpu"   # dev box: no GPU; production overlay re-asserts gpu