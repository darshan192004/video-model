# Copy to `secrets.env`, fill in, chmod 600 (gitignored). OUT-OF-BAND chatter:
# these are production values issued by the org Dex / DB admin — never commit.
# Generate SESSION/POSTGRES secrets with:  openssl rand -hex 32 |  openssl rand -hex 24
OIDC_CLIENT_SECRET=
SESSION_SECRET=
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=media